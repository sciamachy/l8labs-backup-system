#!/bin/bash
#
# This is a dummy module
# It only tells the backup system to include this container
# The main backup script will handle copying the /etc/containers/<container_name> directory

# No special backup function needed
# The main script will automatically handle config backup
